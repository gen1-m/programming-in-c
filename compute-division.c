#include <stdio.h>

int main()
{
    double result; /*The result of calculation*/
    double one = 3 + 1;
    double two = 5; /*Defining the numbers as doubles gives us the desired result.*/

    result = one / two;
    printf("The value of 4/5 is %lf\n", result);
    return (0);
}